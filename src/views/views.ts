import { Label } from "./label"
import { Button } from "./button"
import { Switch } from "./switch"
import { Slider } from "./slider"

export var views = {
    Label,
    Button,
    Switch,
    Slider
}